---
- name: Update registry service
  hosts: registry
  remote_user: root
  roles:
    - registry_service

- name: Update builder service
  hosts: builder
  remote_user: root
  roles:
    - builder_service

- name: Update web service
  hosts: web
  remote_user: root
  roles:
    - web_service
