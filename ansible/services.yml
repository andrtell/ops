---
- name: Install registry service
  hosts: registry
  remote_user: root
  roles:
    - registry_service

- name: Install builder service
  hosts: builder
  remote_user: root
  roles:
    - builder_service

- name: Install web service
  hosts: web
  remote_user: root
  roles:
    - web_service
